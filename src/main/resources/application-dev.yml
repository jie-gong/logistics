spring:
  datasource:
    username: root
    password: 1234
    url: jdbc:mysql://localhost:3306/logistics?characterEncoding=UTF8&autoReconnect=true&useSSL=true&serverTimezone=UTC
    driver-class-name: com.mysql.cj.jdbc.Driver
  #整合mybatis

mybatis-plus:
  mapper-locations: classpath:mybatis/mapper/*.xml

pagehelper:
  helper-dialect: mysql
  reasonable: true
  support-methods-arguments: true
  params: count=countSql